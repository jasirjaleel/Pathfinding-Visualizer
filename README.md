# Pathfinding Visualizer

A Python project using **Pygame** to visualize common pathfinding algorithms like **Breadth-First Search (BFS)**, **Dijkstra’s Algorithm**, and **A\***. This tool allows you to interactively set starting points, target points, and obstacles, then watch as the algorithm finds the shortest path between the start and target.